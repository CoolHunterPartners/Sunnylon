import 'package:flutter/material.dart';

class SunnylonColors {
  static const primary = Color(0xFFA04ABE);      // morado principal (botones / acentos)
  static const background = Color(0xFFF5F5F5);   // fondo general de la app
  static const cardBorder = Color(0xFF4F88FF);   // borde azul/violeta de la tarjeta (aprox.)
  static const textPrimary = Color(0xFF1B1B1B);  // títulos
  static const textSecondary = Color(0xFF7E7E7E); // subtítulos y textos secundarios
  static const inputBackground = Color(0xFFF9F9F9);
}
